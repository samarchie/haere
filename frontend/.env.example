# Not needed for local dev (src/config.ts defaults to "/data", matching
# vite.config.ts's dev-only data proxy). Set this as a real environment
# variable in the deploy platform (e.g. Cloudflare Pages build settings) for
# production — never commit an actual .env file with the real value.
VITE_DATA_BASE_URL=https://data.haere.nz
